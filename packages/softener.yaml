softener:
  # ===================================================================
  # WATER SOFTENER MONITORING PACKAGE
  # ===================================================================
  # Мониторинг циклов умягчения воды по потреблению электроэнергии
  #
  # Логика:
  # - Нормальное потребление: 1-2 Вт (режим ожидания)
  # - Цикл умягчения: значительно выше (определяется автоматически)
  # - Уведомление при завершении цикла
  #
  # Установка:
  # 1. Создайте файл: config/packages/water_softener.yaml
  # 2. Перезапустите Home Assistant
  # ===================================================================

  # -------------------------------------------------------------------
  # Input Number (настройки порогов)
  # -------------------------------------------------------------------
  input_number:
    water_softener_threshold:
      name: "Порог начала цикла умягчения"
      min: 3
      max: 100
      step: 1
      initial: 10
      unit_of_measurement: "W"
      icon: mdi:gauge
      mode: box

    water_softener_min_duration:
      name: "Минимальная длительность цикла"
      min: 1
      max: 60
      step: 1
      initial: 5
      unit_of_measurement: "мин"
      icon: mdi:timer
      mode: box

  # -------------------------------------------------------------------
  # Input Boolean (флаги состояния)
  # -------------------------------------------------------------------
  input_boolean:
    water_softener_cycle_active:
      name: "Цикл умягчения активен"
      icon: mdi:water-sync
      initial: false

    water_softener_notification_sent:
      name: "Уведомление о цикле отправлено"
      icon: mdi:email-check
      initial: false

  # -------------------------------------------------------------------
  # Input Datetime (время начала цикла)
  # -------------------------------------------------------------------
  input_datetime:
    water_softener_cycle_start:
      name: "Начало цикла умягчения"
      has_date: true
      has_time: true
      icon: mdi:clock-start

    water_softener_last_cycle:
      name: "Последний цикл умягчения"
      has_date: true
      has_time: true
      icon: mdi:clock-check

  # -------------------------------------------------------------------
  # Template Sensors (статистика)
  # -------------------------------------------------------------------
  template:
    - sensor:
        # Статус умягчителя
        - name: "Water Softener Status"
          unique_id: water_softener_status
          icon: >
            {% if is_state('input_boolean.water_softener_cycle_active', 'on') %}
              mdi:water-sync
            {% else %}
              mdi:water-check
            {% endif %}
          state: >
            {% if is_state('input_boolean.water_softener_cycle_active', 'on') %}
              Идёт цикл умягчения
            {% else %}
              Режим ожидания
            {% endif %}
          attributes:
            current_power: "{{ states('sensor.0_salt_power') }} W"
            threshold: "{{ states('input_number.water_softener_threshold') }} W"
            cycle_active: "{{ is_state('input_boolean.water_softener_cycle_active', 'on') }}"
            last_cycle: "{{ states('input_datetime.water_softener_last_cycle') }}"

        # Длительность текущего цикла
        - name: "Water Softener Cycle Duration"
          unique_id: water_softener_cycle_duration
          unit_of_measurement: "мин"
          icon: mdi:timer-outline
          state: >
            {% if is_state('input_boolean.water_softener_cycle_active', 'on') %}
              {% set start = states('input_datetime.water_softener_cycle_start') %}
              {% if start not in ['unknown', 'unavailable', ''] %}
                {{ ((as_timestamp(now()) - as_timestamp(start)) / 60) | round(0) }}
              {% else %}
                0
              {% endif %}
            {% else %}
              0
            {% endif %}

        # Дней с последнего цикла
        - name: "Days Since Last Softening"
          unique_id: days_since_last_softening
          unit_of_measurement: "дн"
          icon: mdi:calendar-clock
          state: >
            {% set last = states('input_datetime.water_softener_last_cycle') %}
            {% if last not in ['unknown', 'unavailable', ''] %}
              {{ ((as_timestamp(now()) - as_timestamp(last)) / 86400) | round(1) }}
            {% else %}
              unavailable
            {% endif %}

  # -------------------------------------------------------------------
  # Automation (автоматизации)
  # -------------------------------------------------------------------
  automation:
    # 1. НАЧАЛО ЦИКЛА УМЯГЧЕНИЯ (повышение потребления)
    # -----------------------------------------------------------------
    - id: "water_softener_cycle_start"
      alias: "Умягчитель: Начало цикла"
      description: "Определяет начало цикла умягчения по росту потребления"

      trigger:
        - platform: numeric_state
          entity_id: sensor.0_salt_power
          above: input_number.water_softener_threshold
          for:
            seconds: 30 # Ждём 30 секунд для уверенности

      condition:
        # Цикл ещё не активен
        - condition: state
          entity_id: input_boolean.water_softener_cycle_active
          state: "off"

        # Датчик доступен
        - condition: template
          value_template: "{{ states('sensor.0_salt_power') not in ['unavailable', 'unknown'] }}"

      action:
        # Записываем время начала цикла
        - service: input_datetime.set_datetime
          target:
            entity_id: input_datetime.water_softener_cycle_start
          data:
            datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"

        # Активируем флаг цикла
        - service: input_boolean.turn_on
          target:
            entity_id: input_boolean.water_softener_cycle_active

        # Сбрасываем флаг уведомления
        - service: input_boolean.turn_off
          target:
            entity_id: input_boolean.water_softener_notification_sent

        - service: logbook.log
          data:
            name: "Умягчитель воды"
            message: >
              💧 Начало цикла умягчения. Мощность: {{ states('sensor.0_salt_power') }} Вт

    # 2. ЗАВЕРШЕНИЕ ЦИКЛА УМЯГЧЕНИЯ (возврат к низкому потреблению)
    # -----------------------------------------------------------------
    - id: "water_softener_cycle_complete"
      alias: "Умягчитель: Завершение цикла"
      description: "Определяет завершение цикла по снижению потребления"

      trigger:
        - platform: numeric_state
          entity_id: sensor.0_salt_power
          below: input_number.water_softener_threshold
          for:
            minutes: 2 # Ждём 2 минуты для уверенности

      condition:
        # Цикл должен быть активен
        - condition: state
          entity_id: input_boolean.water_softener_cycle_active
          state: "on"

        # Цикл длился минимум заданное время
        - condition: template
          value_template: >
            {% set start = states('input_datetime.water_softener_cycle_start') %}
            {% set min_duration = states('input_number.water_softener_min_duration') | int %}
            {% if start not in ['unknown', 'unavailable', ''] %}
              {{ ((as_timestamp(now()) - as_timestamp(start)) / 60) >= min_duration }}
            {% else %}
              false
            {% endif %}

        # Уведомление ещё не отправлено
        - condition: state
          entity_id: input_boolean.water_softener_notification_sent
          state: "off"

      action:
        - variables:
            start_time: "{{ states('input_datetime.water_softener_cycle_start') }}"
            duration_minutes: >
              {{ ((as_timestamp(now()) - as_timestamp(start_time)) / 60) | round(0) }}

        # Отправляем уведомление в Telegram
        - service: telegram_bot.send_message
          data:
            config_entry_id: !secret telegram_entry_id_info
            target:
              - !secret chat_info
            title: "💧 ЦИКЛ УМЯГЧЕНИЯ ЗАВЕРШЁН"
            message: >-
              ✅ Умягчение воды успешно произведено!

              📅 Дата: {{ now().strftime('%d.%m.%Y') }}
              🕐 Время завершения: {{ now().strftime('%H:%M:%S') }}
              ⏱️ Длительность цикла: {{ duration_minutes }} мин
              🔌 Текущее потребление: {{ states('sensor.0_salt_power') }} Вт

              🌊 Система готова к работе.

        # Записываем время последнего цикла
        - service: input_datetime.set_datetime
          target:
            entity_id: input_datetime.water_softener_last_cycle
          data:
            datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"

        # Деактивируем флаг цикла
        - service: input_boolean.turn_off
          target:
            entity_id: input_boolean.water_softener_cycle_active

        # Устанавливаем флаг отправки уведомления
        - service: input_boolean.turn_on
          target:
            entity_id: input_boolean.water_softener_notification_sent

        - service: logbook.log
          data:
            name: "Умягчитель воды"
            message: >
              ✅ Цикл умягчения завершён. Длительность: {{ duration_minutes }} мин

    # 3. ПРОВЕРКА ДОСТУПНОСТИ ДАТЧИКА
    # -----------------------------------------------------------------
    - id: "water_softener_sensor_check"
      alias: "Умягчитель: Проверка датчика"
      description: "Уведомляет о недоступности датчика мощности"

      trigger:
        - platform: state
          entity_id: sensor.0_salt_power
          to:
            - "unavailable"
            - "unknown"
          for:
            minutes: 15

      action:
        - service: telegram_bot.send_message
          data:
            config_entry_id: !secret telegram_entry_id_info
            target:
              - !secret chat_warn
            title: "⚠️ ПРОБЛЕМА С ДАТЧИКОМ УМЯГЧИТЕЛЯ"
            message: >-
              🚨 Датчик мощности умягчителя недоступен!

              📍 Устройство: sensor.0_salt_power
              📊 Статус: {{ states('sensor.0_salt_power') }}
              ⏰ Время: {{ now().strftime('%d.%m.%Y %H:%M:%S') }}

              {% if is_state('input_boolean.water_softener_cycle_active', 'on') %}
              ⚠️ Цикл умягчения был активен. Проверьте устройство!
              {% endif %}

              Возможные причины:
              • Отключено питание розетки
              • Проблемы с Wi-Fi
              • Розетка вышла из строя

        - service: logbook.log
          data:
            name: "Умягчитель воды"
            message: "⚠️ ВНИМАНИЕ: Датчик мощности недоступен"

    # 4. ВОССТАНОВЛЕНИЕ ДАТЧИКА
    # -----------------------------------------------------------------
    - id: "water_softener_sensor_restored"
      alias: "Умягчитель: Датчик восстановлен"
      description: "Уведомляет о восстановлении датчика"

      trigger:
        - platform: state
          entity_id: sensor.0_salt_power
          from:
            - "unavailable"
            - "unknown"

      condition:
        - condition: template
          value_template: "{{ states('sensor.0_salt_power') not in ['unavailable', 'unknown'] }}"

      action:
        - service: telegram_bot.send_message
          data:
            config_entry_id: !secret telegram_entry_id_info
            target:
              - !secret chat_info
            title: "✅ ДАТЧИК УМЯГЧИТЕЛЯ ВОССТАНОВЛЕН"
            message: >
              ✅ Датчик мощности умягчителя снова работает!

              📊 Текущее потребление: {{ states('sensor.0_salt_power') }} Вт
              ⏰ Время восстановления: {{ now().strftime('%H:%M:%S') }}

              ⚡ Мониторинг возобновлён.

        - service: logbook.log
          data:
            name: "Умягчитель воды"
            message: "✅ Датчик мощности восстановлен"

    # 5. НАПОМИНАНИЕ О ДОЛГОМ ОТСУТСТВИИ ЦИКЛА (опционально)
    # -----------------------------------------------------------------
    - id: "water_softener_cycle_overdue"
      alias: "Умягчитель: Давно не было цикла"
      description: "Напоминает если цикл умягчения давно не проводился"

      trigger:
        - platform: time
          at: "10:00:00" # Проверка раз в день в 10:00

      condition:
        # Прошло больше 35 дней с последнего цикла
        - condition: template
          value_template: >
            {% set last = states('input_datetime.water_softener_last_cycle') %}
            {% if last not in ['unknown', 'unavailable', ''] %}
              {{ ((as_timestamp(now()) - as_timestamp(last)) / 86400) > 35 }}
            {% else %}
              false
            {% endif %}

      action:
        - service: telegram_bot.send_message
          data:
            config_entry_id: !secret telegram_entry_id_info
            target:
              - !secret chat_info
            title: "⚠️ НАПОМИНАНИЕ О УМЯГЧИТЕЛЕ"
            message: >-
              ⏰ Внимание! Цикл умягчения воды давно не проводился.

              📅 Последний цикл: {{ as_timestamp(states('input_datetime.water_softener_last_cycle')) | timestamp_custom('%d.%m.%Y %H:%M') }}
              📊 Прошло дней: {{ states('sensor.days_since_last_softening') }}

              💡 Проверьте:
              • Достаточно ли соли в умягчителе
              • Работает ли устройство корректно
              • Нет ли проблем с водоснабжением

  # ===================================================================
  # НАСТРОЙКА И КАЛИБРОВКА
  # ===================================================================
  #
  # ЭТАП 1: ОПРЕДЕЛЕНИЕ ПОРОГА
  # 1. Посмотрите нормальное потребление (1-2 Вт)
  # 2. Дождитесь цикла умягчения и посмотрите максимальное потребление
  # 3. Установите порог в input_number.water_softener_threshold
  #    Рекомендуется: (Нормальное + Максимальное) / 2
  #    Например: (2 + 50) / 2 = 26 Вт → установите 10-15 Вт для запаса
  #
  # ЭТАП 2: НАСТРОЙКА МИНИМАЛЬНОЙ ДЛИТЕЛЬНОСТИ
  # 1. Узнайте типичную длительность цикла умягчения
  # 2. Установите минимальную длительность в input_number.water_softener_min_duration
  #    Это защитит от ложных срабатываний при кратковременных скачках
  #
  # ЭТАП 3: ТЕСТИРОВАНИЕ
  # 1. Дождитесь следующего цикла умягчения
  # 2. Проверьте, что уведомление пришло корректно
  # 3. При необходимости скорректируйте пороги
  #
  # ТИПИЧНЫЕ ЗНАЧЕНИЯ:
  # - Нормальное потребление: 1-3 Вт
  # - Цикл умягчения: 20-100 Вт
  # - Порог срабатывания: 8-15 Вт
  # - Длительность цикла: 30-120 минут
  #
  # ===================================================================
